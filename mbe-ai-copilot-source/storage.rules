rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // deliverables/{uid}/{fileName} — a user can only read/write files under their own uid folder.
    match /deliverables/{uid}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
